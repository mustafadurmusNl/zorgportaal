import { NotFoundPage } from "@/components/sections";

// Show NotFound page for /clienten root - users should go to specific client pages
export default function ClientenRootPage() {
  return <NotFoundPage />;
}
