"use client";

import { ZorgSimpleContentSection } from "@/components/zorg";

const ZelfbeeldTypes = () => {
  return (
    <ZorgSimpleContentSection translationNamespace="zelfbeeld.sections.types">
      {/* Types content (e.g., low self-worth, body image issues) can be added here */}
    </ZorgSimpleContentSection>
  );
};

export default ZelfbeeldTypes;
